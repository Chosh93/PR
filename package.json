{
  "name": "pr",
  "private": true,
  "scripts": {
    "local:desktop": "pnpm install && turbo local:desktop",
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "generate-barrels": "npx barrelsby --config barrels-config.json"
  },
  "devDependencies": {
    "@pr/eslint-config": "workspace:*",
    "husky": "^8.0.3",
    "prettier": "^3.2.5",
    "turbo": "^2.2.3"
  },
  "packageManager": "pnpm@8.9.0",
  "engines": {
    "node": ">=18"
  }
}
